[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "jhtdb-pipeline"
version = "0.4.0"
description = "SciServer-only JHTDB full-periodic center pipeline"
requires-python = ">=3.9"
dependencies = [
  "filelock>=3.13,<4",
  "giverny<4",
  "numcodecs>=0.12,<0.16",
  "numpy>=1.23,<3",
  "plotly>=5,<7",
  "PyYAML>=6,<7",
  "rich>=13,<15",
  "scipy>=1.9,<2",
  "streamlit>=1.40,<2",
  "zarr>=2.18,<3",
]

[project.scripts]
jhtdb-pipeline = "jhtdb_pipeline.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
