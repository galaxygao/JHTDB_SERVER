# JHTDB 流水线后续性能优化备忘

> 记录日期：2026-08-26
> 本文件仅记录可选优化方向，不代表当前必须修改，也不改变现有程序、配置或数据。

## 1. 当前实现基线

当前程序优先保证低内存、数据准确、可恢复和可追溯：

- 原始速度、9 个原始梯度、滤波速度和滤波梯度分别持久化；
- 原始梯度完成后执行全域无散性验证；
- Zarr 字段写入后回读，并记录 SHA-256 和 manifest；
- FFT 按小 slab 串行处理，默认 `fft_slab_width: 4`、`workers=1`；
- 使用 `Zstd level 3 + bitshuffle` 压缩；
- 不并发请求 JHTDB；
- 不把完整 `1024³` 复数频谱一次性放入内存。

滤波相关的全域轴向变换已经从旧实现的 45 次减少为 27 次：

```text
9 次：3 个速度分量的三维滤波
9 次：3 个滤波速度分别沿 x、y、z 求梯度
9 次：3 个 nonlinear acceleration 分量的三维滤波
```

## 2. 推荐优化顺序

### 2.1 增大 FFT slab

当前 `fft_slab_width=4`，每个轴需要约 `1024/4=256` 批。

可在真实硬件上依次评估：

```text
4 → 8 → 16
```

预期收益：

- 减少 Python 循环和 FFT 调度次数；
- 减少小块 memmap/Zarr I/O；
- 通常是最容易获得的速度提升。

代价：FFT 输入、频谱和输出缓冲会近似随 slab 宽度线性增加。每次调整前必须重新估算并监控峰值 RAM，不应直接跳到很大的 slab。

### 2.2 合并 acceleration 的 slab 内累加

当前定义：

```text
a_i = sum_j(u_j * du_i/dx_j)
```

可在一个 slab 的内存数组中完成三个 `j` 分量的累加，然后一次性写入 `result.f32`。这样可以减少对 result memmap 的重复读取和写入，同时不改变物理定义和数值算子。

### 2.3 合并 resolved work 的局部计算

`work_resolved` 可以按 slab 直接完成：

```text
work_resolved = sum_i[bar(u_i) * sum_j(bar(u_j) * dbar(u_i)/dx_j)]
```

该优化可以减少中间 `result.f32` 的磁盘往返，但需要保持现有分量顺序、float32 输出和验证逻辑。

### 2.4 适度增加 FFT workers

当前本地 FFT 使用 `workers=1`。本地计算不属于 JHTDB 网络查询，因此可以独立评估 `workers=2` 或 `workers=4`。

可能收益：提高 FFT 吞吐量。

风险：

- 增加瞬时 RAM；
- 增加 CPU 和内存带宽竞争；
- 如果瓶颈是磁盘或 Zarr 解压，多 worker 不一定更快。

应先完成单 worker 基准测试，再逐级增加。

### 2.5 减少重复 Zarr chunk 解压

当前 `128³` Zarr chunk 与宽度为 4 的 FFT slab 不完全对齐，可能重复读取和解压相同 chunk。

可评估：

- 增大 slab；
- 显式复用已经解压的相邻 chunk；
- 对连续计算步骤使用受限大小的读取缓存。

缓存必须有明确上限，不能恢复成一次加载完整 3D 场的高内存实现。

### 2.6 调整派生数据压缩

当前使用 `Zstd level 3 + bitshuffle`。可比较：

| 方案 | 速度 | 磁盘占用 | 适用情况 |
|---|---:|---:|---|
| Zstd level 3 | 中等 | 较小 | 当前稳妥默认值 |
| 更低 Zstd 等级 | 较快 | 略大 | CPU 压缩成为瓶颈时 |
| LZ4 | 快 | 较大 | 磁盘空间充足且重视速度时 |

当前剩余空间有限，不应在没有实际压缩率测试的情况下直接切换到低压缩方案。

## 3. 高风险方案

### 完整三维 FFT 频谱复用

理论上，可以对每个速度分量只做一次完整 3D FFT，然后从同一频谱生成：

```text
原始梯度        inverse_FFT(i*k_j*U)
滤波速度        inverse_FFT(H*U)
滤波速度梯度    inverse_FFT(i*k_j*H*U)
```

但 `1024³` 实数场的半谱 complex64 本身约为 4 GiB。考虑输入、频谱、多个输出和 FFT 临时缓冲，单个分量可能需要约 8–16 GiB RAM。当前机器曾因高内存计算崩溃，因此暂不建议采用。

只有在以下条件满足后才应重新评估：

- 有足够物理内存；
- 有经过验证的 out-of-core 或分布式 FFT；
- 小规模结果与现有 slab 算法逐点一致；
- 中断恢复和 manifest 管理能力没有退化。

## 4. 不应为了速度删除的功能

以下功能属于数据正确性保障，不作为性能优化删除项：

- 原始速度和梯度 manifest 绑定；
- 9 个原始梯度的持久化；
- 全域无散性 RMS 与 maximum 双门槛；
- 小块 FD8/FFT 独立审计；
- Zarr 写入后的回读检查；
- SHA-256 字段校验；
- 重复数据和时间索引管理；
- 断点恢复与未完成字段重算；
- 周期边界、坐标轴和分量顺序测试。

## 5. 建议的评估方法

每项优化应单独评估，不同时改变多个参数。至少记录：

```text
time_index
算法和配置版本
slab width
FFT workers
压缩算法和等级
阶段耗时
进程峰值 RAM
磁盘读写量
输出文件实际大小
输入/输出 manifest hash
无散性报告
与基线结果的 RMS 和最大绝对差异
```

推荐决策顺序：

1. 先以当前设置完成一个时间帧，取得真实基线；
2. 只增加 slab width，比较时间和 RAM；
3. 再评估 acceleration/resolved work 的 I/O 合并；
4. 最后评估 FFT workers 和压缩参数；
5. 只有常规优化仍无法满足需求时，才考虑完整 3D FFT、GPU 或分布式方案。
